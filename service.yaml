apiVersion: v1
kind: Service
metadata:
  name: online-book-store-service
spec:
  type: NodePort
  selector:
    app: bookstore
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007